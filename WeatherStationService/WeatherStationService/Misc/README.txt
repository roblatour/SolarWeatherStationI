use Visual Studio 2019 (or above) to compile a release version of the WeatherStationService

once compiled, setting are manually configured in the file bin\Release\WeatherStationService.exe.config

the _install.bat file installs the compiled service; it will auto start

run the _install.bat, _uninstall.bat, _start.bat, and stop.bat files as an administrator

